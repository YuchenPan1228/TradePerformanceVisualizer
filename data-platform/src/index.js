import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import SnapTradeExplorer from './SnapTradeExplorer';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <SnapTradeExplorer />
  </React.StrictMode>
);
